<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="style.css">
    <title>To Do List</title>
    <style>
    body {background-color : gainsboro;}
    a:link {
        color: whitesmoke;
        text-decoration: none;
    }
    a:visited {
        color: whitesmoke;
        text-decoration: none;
    }
    #buttons {
        position:relative;
        top:-50%;
        display: flex;
        justify-content: flex-end;
    }
    #tasks {
        height: 9vh;
        font-size: 20px;
        padding: 12px 20px;
        margin: 1px 0;
        box-sizing: border-box;
    }
    #taskReference {
        height: 9vh;
        background-color: ghostwhite;
        font-size: 20px;
        padding: 12px 20px;
        margin: 1px 0;
        box-sizing: border-box;
    }
    #taskDate {
        font-family: "Lucida Handwriting",Cursive;
    }
    #buttons {
        position:relative;
        top:-50%;
        display: flex;
        justify-content: flex-end;
    }
    #addButton {
        background-color: #4CAF50; /* Green */
        border: none;
        position: absolute;

        left:68%;
        width:100px;
        height:35px;
        color: white;
        padding: 7px 16px;
        text-align: center;
        text-decoration: none;
        display: inline-block;
        font-size: 16px;
    }
    #buttonEditar {
        background-color: cornflowerblue;
        border: 2px solid darkslategrey;
        position: absolute;
        width:50px;
        height:20px;
        color: white;
        padding: 8px 17px;
        text-align: center;
        text-decoration: ghostwhite;
        display: inline-block;
        font-size: 16px;
    }
    #buttonApagar {
        background-color: red;
        border: 2px solid darkred;
        position: absolute;
        right: -15%;
        width:50px;
        height:20px;
        color: white;
        padding: 8px 17px;
        text-align: center;
        text-decoration: ghostwhite;
        display: inline-block;
        font-size: 16px;
    }
    #container {
        width: 100vw;
        height: 9vh;
        display: flex;
        flex-direction: row;
        justify-content: center;
        align-items: center;
     }
     input[type=text] {
         width: 100%;
         padding: 12px 20px;
         margin: 1px 0;
         box-sizing: border-box;
     }
    </style>
</head>
<body>
     <% if (taskDelete != null) { %>
        <section id="modal" style="background-color: ghostwhite;font-size: 20px;border: 2px solid darkslategrey;display: inline-block;text-align: center;width: 100vw;">
             </div >
                <div id="card">
                    <a href="/"></a>
                    <h2 style="font-family: Cursive" >Confirmação</h2>
                </div>
                <p style="font-family: Cursive;">
                   Deseja realmente apagar essa tarefa: "<%= taskDelete.task %>"?
                   <b style="font-family: Cursive">a ação não poderá ser desfeita </b>
                </p>
                <a href="/" style="font-family: Cursive;background-color: red;border: 2px solid darkslategrey">Cancelar</a>
                <a href="/deleteOne/<%= taskDelete._id %>" style="font-family: Cursive;background-color: green;border: 2px solid darkslategrey">Confirmar</a>
            </div>
        </section>
        <% } %>
   <div id = "container">
    <% if (task == null) { %>
           <form action="/create" method="post">
               <input type="text" name="task" id="task" placeholder="Insira uma tarefa">
               <button id="addButton" type="submit">Adicionar</button>
           </form>
           <% } else { %>
           <form action="/updateOne/<%= task._id %>" method="post">
               <input type="text" name="task" id="task" value="<%= task.task %>">
               <button id="addButton" type="submit">Atualizar</button>
           </form>
           <% } %>
   </div>

    <section id="tasks">
        <ul>
            <% tasksList.forEach((task)=>{ %>
                <li id = "taskReference">
                    <div id= "taskDate">
                     <%= task.task %> | <%= task.date.toLocaleDateString('pt-BR') %>
                    </div>
                    <div id = buttons>
                         <div id ="buttonEditar">
                             <a href="/getById/<%= task._id %>/update">Editar</a>
                         </div>
                         <div id = "buttonApagar">
                             <a href="/getById/<%= task._id %>/delete">Apagar</a>
                         </div>
                    </div>

                </li>
            <% }) %>
        </ul>

    </section>



</body>
</html>